vim.g.barbar_auto_setup = false

require("barbar").setup({
  animation = false,
  auto_hide = false,
  focus_on_close = "left",
  clickable = true,
  maximum_padding = 1,
  minimum_padding = 1,
  maximum_length = 30,
  icons = {
    buffer_index = false,
    buffer_number = false,
    filetype = { enabled = true },
    button = "",
    separator = { left = "▎", right = "" },
    modified = { button = "●" },
    pinned = { button = "" },
    preset = "default",
  },
  -- this removes duplicate "init.lua" issue
  name_formatter = function(buf)  
    return vim.fn.fnamemodify(buf.path, ":t") -- show only filename
  end,
})

